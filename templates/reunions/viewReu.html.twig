{% extends 'base.html.twig' %}

{% block title %}Détails d'une réunion{% endblock %}

{% block content %}
	<section class="py-2 mb-5">
		<a href="{{ path('index_reunions') }}" class="btn btn-secondary mt-4 float-left" role="button"><i class="fas fa-angle-left"></i> Retour à la liste des réunions</a>
	    <h2>Détails de réunion  </h2>
		<div class="card-deck">
			<div class="card mb-3">
			    <div class="card-header">
			        <div class="card-title"><b>{{ reunionTrouvee.getTitre() }}</b> du {{ reunionTrouvee.getDatetimeReu()|date("d/m/Y") }} à {{ reunionTrouvee.getDatetimeReu()|date("H:i") }} (<em>{{ reunionTrouvee.getTypeReu() }}</em><br>Ordre du jour : {{ reunionTrouvee.getContenu() }}
			        	
		  			<div class="card-body">
 	
				            Intitulé de la réunion
				            Type de réunion
				            Date et heure 
				            Lieux
				            Contenu de la réunion
				            Proposé par 
				            Compte-rendu 
				          
				         </div>
					  </div>
			            
			          
			        
			        
			          
			            {{ reunionTrouvee.getTitre() }} 
			            
			            Le  
			            {{ reunionTrouvee.getLieuReu() }} 
			             
			            {{ reunionTrouvee.getUser().getPrenom() ?? '' }} 
			            {{ crTrouve.getUser().getPrenom() ?? '' }} 

			        </div>  
			            	{% if crTrouve is not null %}
			            	<a class="btn btn-success btn-block" href="{{ path('edit_cr', {id : crTrouve.getId()}) }}" role="button">Modifier le CR</a>
			            	{% else %}
			            	<a class="btn btn-success btn-block" href="{{ path('add_cr', {id : reunionTrouvee.getId()}) }}" role="button">Ajouter le CR</a>
			            	<a class="btn btn-primary btn-block" href="{{ path('edit_reunion', {id : reunionTrouvee.getId()}) }}" role="button">Modifier</a>
			            	{% endif %}
			            	<a class="btn btn-danger btn-block" href="{{ path('del_reunion', {id : reunionTrouvee.getId()}) }}" role="button">Supprimer</a>
			            
			        

			      </div>
			    </div>
			</div>
			<div class="card-footer small text-muted">Reunion postée le  {{reunionTrouvee.getDatetimePost()|date("d/m/Y")}}</div>
		</div>
	</section>
{% endblock %}
